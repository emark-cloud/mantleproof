{
  "name": "@mantleproof/agents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Three agent-to-agent demo scripts (deployer / trading / yield)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo \"no tests yet (scaffold)\" && exit 0",
    "demo:deployer": "tsx src/deployer-agent.ts",
    "demo:trading": "tsx src/trading-agent.ts",
    "demo:yield": "tsx src/yield-agent.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
