"""Named bytecode pattern registry (lb_no_bin_validation_v1 ...).
SCAFFOLD — implement in T8."""

from __future__ import annotations


def match_patterns(code: bytes) -> list:
    raise NotImplementedError("SCAFFOLD: T8")
